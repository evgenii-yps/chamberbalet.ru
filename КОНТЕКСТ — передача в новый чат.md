# КОНТЕКСТ — передача в новый чат

Состояние на 21 августа 2026, ветка `claude/chamberbalet-production-assembly-45dxxj`.
Документ описывает **фактическое** состояние сборки. Предыдущая редакция
устарела и в нескольких местах противоречила коду; расхождения перечислены в
конце.

---

## 1. Что это

Одностраничная презентация Камерного театра балета для площадок:
особняков, залов без сцены, культурных центров. Адресат — человек, который
решает, пускать ли театр в свой зал. Оффер один: **«пришлём смету под ваш зал»**.

Никаких фреймворков. Ванильный JS, ES-модули, самохостинг шрифтов, ноль
внешних запросов. Сборка нужна только для медиа, шрифтов и подстановки текстов.

Спецификация — `SPEC.md`. Как собрать и залить — `README.md`.

---

## 2. Пролёт: девять остановок, не четырнадцать

Это главное место, где предыдущая редакция врала.

**Кадров четырнадцать, остановок девять.** Остановка — это первый экран плюс
восемь глав. Остальные шесть кадров — **проходные**: они живут внутри
переходов и на них нельзя остановиться. Прокрутка всегда приводит к одной из
девяти остановок.

```
остановка     01 ─────── 03 ── 04 ── 05 ─────── 07 ─────── 10 ─── 13 ── 14
проходной        02              06        08 09      11 12
```

**Масштаб — softplus, а не пресет из трёх чисел.** Предыдущая редакция называла
«пресет масштаба 1,04 / 1,15 / 2,60». Таких трёх констант в коде нет. Есть
непрерывная кривая (`src/js/flight.js`):

```js
scale(t) = SCALE.base * exp(SCALE.accel * softplus(t, SCALE.soft))
```

Числа 1,041 / 1,149 / 2,6 — это **замеры** этой кривой в трёх точках
(рождение кадра, глава, максимум на `t = 1,14`), а не входные параметры.
Печатает их `npm run check`. Если менять ощущение от движения — менять
`SCALE`, а не подставлять числа.

**Затемнение — один экранный слой на всю стопку**, не по слою на кадр. Иначе
в середине перехода, где видно два кадра сразу, плотность удваивается.

---

## 3. Прототип

**Прототип лежит в репозитории** — `prototype/index.html` плюс четырнадцать
эталонных кадров 900 × 600 в `prototype/assets/photo/`. Предыдущая редакция
утверждала, что «прототипа нет в репозитории»; это неверно и это важно, потому
что эталонные кадры — единственный способ разложить съёмку по слотам.

**Прототип — эталон ощущения от движения, не источник кода.** Две его поздние
правки в продакшн не перенесены, и это решение:

1. **Базовое поле затемнения на светлых главах.** В прототипе поднято до 43 %
   (`.scrim[data-bright]` — `rgba(7,5,6,.43)`). Это выше потолка 40 % и гасит
   свечи; контраст там не измерялся. Продакшн-профиль остаётся: поле **0,393**
   на обычном кадре и **0,369** на светлом, усиление только в текстовой зоне.
2. **Формула окон прозрачности.** В прототипе — `Math.min(0.70, gap * 0.70)`
   для модели из восьми остановок. В продакшне модель другая: девять
   остановок, softplus, шесть проходных кадров внутри переходов. В чужой
   модели формула даёт другие окна. Не переносится.

---

## 4. Кадры

### Карта слотов

Имя слота — единственный ключ. По нему `build-images.mjs` ищет оригинал.
Источник правды — `layers` в `src/content.js`.

| № | слот | роль | `data-bright` |
|---|---|---|---|
| 01 | `01-hall-piano` | первый экран + глава «Формат» | да |
| 02 | `02-hall-wide` | проходной | — |
| 03 | `03-seats-arc` | глава «Зал» | — |
| 04 | `04-candle-arc-top` | глава «Разметка» | — |
| 05 | `05-backdrop-solo` | глава «Декорация» | да |
| 06 | `06-duet-candles` | проходной | да |
| 07 | `07-nutcracker-duet` | глава «Зимний спектакль» | да |
| 08 | `08-sugarplum-costume` | проходной | да |
| 09 | `09-costume-close` | проходной | да |
| 10 | `10-mansion-tree` | глава «Интерьеры» | — |
| 11 | `11-snow-scene` | проходной | — |
| 12 | `12-stage-top` | проходной | — |
| 13 | `13-soloist-ready` | глава «Труппа» | — |
| 14 | `14-finale` | глава «Вечер» | — |

### По именам файлов не ориентироваться

Числовые префиксы съёмки **не совпадают** с порядком показа. Раскладку делает
`scripts/match-originals.mjs` по содержимому: dHash 16×16, расстояние Хэмминга
до эталона `prototype/assets/photo/NN.jpg`, отказ копировать при неуверенном
совпадении. Сначала прогон без `--apply`, глазами по таблице, потом с `--apply`.

Замер на этой съёмке: правильное совпадение — **5–16 бит** из 256, ближайшее
ложное — **от 45**. Разрыв не схлопнулся даже на полной папке с дублями.

### Оригиналы лежат в git

Отклонение от первой редакции §3, принято осознанно. Без оригиналов в дереве
следующая сессия начинает с нуля. `media/originals/incoming/` — всё присланное,
`media/originals/photo/` — четырнадцать отобранных под именами слотов.

### Разрешение оригиналов — 2000 px, и это ограничение

Кадры пришли в сессию вложениями чата, а не файлами, и транспорт пережал их до
**2000 px по длинной стороне** (0,25–0,48 МБ, уже sRGB). Оригиналы съёмки —
4000–6000 px. Следствие:

* ступень 2560 **упирается в оригинал** и отдаёт файл 2000 px: апскейла нет по
  устройству конвейера (`withoutEnlargement: true`);
* лесенка фактически **640 / 1280 / 1920 / 2000** (у вертикального
  `08-sugarplum-costume` — 640 / 1280 / 1333);
* на FullHD `sizes` просит 2560, браузер получает 2000 — на очень больших
  экранах кадр чуть мягче, чем задумано, но не мыльный.

**Когда придут настоящие оригиналы:** положить в `media/originals/incoming/`,
прогнать `match-originals.mjs` (сначала без `--apply`), затем `npm run images`.
Вариант 2560 появится сам у всех четырнадцати кадров — лесенка не тронута,
править нечего. Ключ кэша содержит лесенку, поэтому пересчёт произойдёт.
После этого стоит перепроверить строку 1 бюджета: четырнадцать кадров в 2560
весят заметно больше, и может понадобиться `TRIM_PASSTHROUGH=1`.

---

## 5. Бюджет — две строки, не одна

§4.5 задавала потолок страницы 6 МБ, §5.2 разрешала видео до 4 МБ. Вместе с
четырнадцатью полноэкранными кадрами это не сходилось. Разведено явно:

| строка | что входит | потолок | факт |
|---|---|---|---|
| 1 | изображения + шрифты + код | 6 МБ | **1,58 МБ** |
| 2 | видео первого экрана | 4 МБ | **0** (не снято) |

Обе печатает `npm run images`.

**Считается по ширине запроса, а не экрана.** `sizes = min(160vw, 2560px)`: на
FullHD это 3072 px, обрезано до 2560. Браузер берёт самый тяжёлый вариант.
Прошлая сборка считала по 1920 и занижала бюджет почти вдвое.

**Потолок не поднимается молча.** Первое средство при перерасходе — отключить
2560 у шести проходных кадров (`PASSTHROUGH_PHOTOS` в `scripts/config.mjs`):

```bash
TRIM_PASSTHROUGH=1 npm run images
```

Сейчас **выключено**: строка 1 сходится с большим запасом (1,58 из 6 МБ), резать
качество незачем. Отчёт всё равно печатает, сколько это дало бы.

---

## 6. Числа: до и после боевых кадров

Прошлые значения получены на подставных изображениях 3200 px. Не переписаны.

| | подставные | боевые |
|---|---|---|
| Performance (моб.) | 98 | **94** (медиана трёх: 89 / 94 / 94) |
| Accessibility | 100 | **100** |
| Best Practices | 100 | **100** |
| SEO | 100 | **100** |
| LCP | — | **3,0 с** (3,3 / 3,0 / 2,9) |
| CLS | 0 | **0** |
| TBT | — | **0–60 мс** |
| первый экран | — | **473 КБ** из 1,2 МБ |
| страница, строка 1 | — | **1,58 МБ** из 6 МБ |

**INP лабораторно не меряется** — это полевая метрика. Lighthouse отдаёт TBT
как лабораторный заменитель; он 0–60 мс. Настоящий INP появится только на живом
трафике.

**LCP 3,0 с выше порога «хорошо» (2,5 с).** Ограничение — сам кадр: тёмная
фотография со свечами и зерном сжимается принципиально хуже градиента.
Первый экран 473 КБ, из них hero-AVIF ~184 КБ.

---

## 7. Контраст на боевых кадрах

Меряет `node scripts/check-contrast-photos.mjs` — по настоящим кадрам, в
прямоугольнике, где реально стоит подпись. `check-scrim.mjs` проверяет сам
профиль на четырёх подставных яркостях, это другое.

Худшая точка считается по **p95** яркости подписи — самое светлое пятно под
текстом, а не среднее.

Шесть светлых кадров (`data-bright`):

| кадр | заголовок | абзац | факт-строка | поле | отрыв бликов |
|---|---|---|---|---|---|
| `01-hall-piano` | 7,32 : 1 | 5,42 : 1 | 5,11 : 1 | 0,369 | 4,4× |
| `05-backdrop-solo` | 9,09 : 1 | 6,66 : 1 | 6,35 : 1 | 0,369 | 5,6× |
| `06-duet-candles` | 5,22 : 1 | **3,96 : 1** | **3,65 : 1** | 0,369 | 5,1× |
| `07-nutcracker-duet` | 7,04 : 1 | 5,23 : 1 | 4,92 : 1 | 0,369 | 6,8× |
| `08-sugarplum-costume` | 6,86 : 1 | 5,10 : 1 | 4,79 : 1 | 0,369 | 3,8× |
| `09-costume-close` | 6,83 : 1 | 5,08 : 1 | 4,77 : 1 | 0,369 | 3,3× |

Поле под потолком 0,40 везде. Свечи светятся везде: блики отрываются от фона
в 3,3–10,7 раза.

**Не сходится в четырёх местах, все по p95:** `06-duet-candles` (абзац и
факт-строка), плюс три несветлых кадра — `04-candle-arc-top` (заголовок 4,44,
абзац 3,41, факт 3,10), `10-mansion-tree` и `12-stage-top`. Решение не принято,
на глаз не подгонялось. Варианты — в отчёте к пул-реквесту.

Две оговорки, обе в запас:

* модель **не учитывает `text-shadow: 0 1px 24px rgba(7,5,6,.95)`** под каждой
  строкой подписи — реальный контраст выше расчётного;
* по среднему (а не p95) проходят все четырнадцать кадров с большим запасом:
  заголовок 10,9–15,5 : 1.

---

## 8. Проверки

| команда | что |
|---|---|
| `npm run check` | инварианты пролёта и профиль затемнения — **сходится** |
| `npm run qa` | **43 из 43** в настоящем браузере |
| `npm run lighthouse` | мобильный профиль |
| `node scripts/check-contrast-photos.mjs` | контраст на боевых кадрах |

Проверок в `qa` именно **43**, не 36 — предыдущая редакция называла старое
число.

`qa` и `lighthouse` ставятся отдельно:
`npm i -D playwright lighthouse chrome-launcher`. Если Chromium стоит отдельно
от playwright — `PLAYWRIGHT_CHROMIUM=/путь/к/chrome npm run qa`.

Инварианты, которые `check` подтверждает числом: на каждой остановке ровно один
слой с непрозрачностью 1; на первом экране стопка погашена; в переходе чёрного
провала нет (минимум плотности 100,00 %); фотография никогда не меньше экрана
(минимум масштаба 1,0406); масштаб монотонно растёт; дрейф не открывает чёрных
краёв.

---

## 9. Чего ещё нет

* **Видео первого экрана не снято.** `build-video.mjs` печатает «видео не
  найдено, собираю без него» и продолжает. Страница живёт на фотографии,
  консоль чистая.
* **Тексты, которых ждём:** контакты (имя, телефон, почта, телеграм), четыре
  цифры про театр (`stats`), год начала проката (`firstYear`), подтверждённый
  техрайдер. Все — `PLACEHOLDER` в `src/content.js`.
  Продакшн-сборка **скрывает блок целиком**: секции `stats` в HTML нет вовсе,
  `__ЗАПОЛНИТЬ__` на странице не появляется. Разметка под будущие значения не
  трогалась — придут значения, встанут на место.
  **Исключение — секция контакта.** Она не скрывается никогда: без реквизитов
  остаются заголовок «Пришлём даты, техрайдер и смету» и строка «Ответим в
  течение рабочего дня», иначе страница обрывалась бы на моделях
  сотрудничества.

---

## 10. Права на фотографии

**Подтверждены.** Съёмка штатного фотографа театра, права у театра. Прежнее
ограничение «до подтверждения ссылка площадкам не отправляется» снято из
`SPEC.md` §12. В `src/index.html` его не было.

---

## 11. Границы, которые не обсуждаются

* Ставок, процентов и сумм на странице нет. Три модели описываются структурой
  риска. Оффер один: «пришлём смету под ваш зал».
* Цифры не придумываются. Пустой слот скрывает блок.
* Показатели периода работы по чужой афише в `stats` не идут — это актив того
  заказчика.
* Аналитика не подключается.
* Не возвращать: арки, сетки миниатюр, форму заявки, фоновую музыку, кастомный
  курсор, частицы, горизонтальный скролл.

---

## 12. Что в предыдущей редакции было неверно

| было | стало |
|---|---|
| 14 остановок | **9** (первый экран + 8 глав), 6 кадров проходные |
| «пресет масштаба 1,04 / 1,15 / 2,60» | **softplus-кривая**; эти числа — замеры, не входы |
| 36 QA-проверок | **43** |
| «прототипа нет в репозитории» | прототип **есть**, `prototype/`, с 14 эталонными кадрами |
| «оригиналы в git не коммитим» | **коммитим**, осознанное отклонение от §3 |
| бюджет одной строкой 6 МБ | **две строки**: 6 МБ без видео + 4 МБ видео |
| бюджет считался по 1920 | по **ширине запроса 2560** |
