node_modules/
dist/
.build/
.DS_Store

# Деплой-архив: артефакт упаковки dist/, в репозиторий не идёт.
# Имя по схеме chamberbalet-<дата>-<короткий хеш>.zip — собирается заново
# из любого коммита, хранить его в истории нечего и незачем.
chamberbalet-*.zip
